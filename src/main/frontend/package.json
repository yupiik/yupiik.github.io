{
  "private": true,
  "description": "Main UI",
  "scripts": {
    "build": "node ./esbuild",
    "serve": "mvn yupiik-tools:serve-minisite -f ../../../pom.xml",
    "watch": "NODE_ENV=dev npm run build"
  },
  "devDependencies": {
    "postcss": "^8.3.6",
    "postcss-modules": "^4.2.2",
    "preact": "^10.6.4",
    "esbuild": "^0.14.9",
    "fs-extra": "^10.0.0",
    "tmp": "^0.2.1",
    "file-saver": "^2.0.5",
    "jszip": "^3.7.1"
  },
  "dependencies": {}
}
